#!/usr/bin/env bash
#log_file="access.log"



log_file="access.log"



awk '{ count[$1" "$9]++ } END { for (entry in count) printf "%-10s %s\n", count[entry], entry }' "$log_file" | sort -nr






#awk '{ print $1, $9 }' | uniq -c | sort -nr | head -n 10 
